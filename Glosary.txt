1️⃣ Flaky test

Що означає: тест, який то проходить, то падає без змін у коді
Чому так кажуть: flaky = нестабільний, крихкий
У розмові:

“This test is flaky because of timing issues.”

2️⃣ Cache / Cached WebElement

Що означає: ти зберіг посилання на WebElement і використовуєш його пізніше
Проблема: DOM оновився → посилання застаріло
У розмові:

“Don’t cache WebElements, re-locate them.”

3️⃣ Stale element

Що означає: WebElement більше не прив’язаний до поточного DOM
Коли виникає: refresh, SPA update, dynamic render
У розмові:

“We got a stale element after page refresh.”

4️⃣ Explicit wait

Що означає: чекати конкретну умову для конкретного елемента
Чому важливо: контроль + менше flaky
У розмові:

“Use explicit waits instead of sleep.”

5️⃣ Implicit wait

Що означає: глобальний таймаут на всі findElement
Чому не люблять: приховані затримки, складно дебажити
У розмові:

“Implicit wait caused unpredictable delays.”

6️⃣ Thread.sleep()

Що означає: тупо “засни на N секунд”
Чому погано:

не чекає умову

робить тести повільними
У розмові:

“Remove Thread.sleep, it makes tests flaky.”

7️⃣ State-aware action

Що означає: дія з перевіркою стану
Приклад:

не клікати checkbox, якщо він уже selected
У розмові:

“Make the click state-aware.”

8️⃣ Blind click

Що означає: клік без перевірки, чи можна клікати
Результат: flaky tests
У розмові:

“That’s a blind click, add a wait.”

9️⃣ Locator strategy

Що означає: як саме ти вибираєш елемент (CSS, XPath, id…)
У розмові:

“We need a better locator strategy.”

🔟 Unique locator (1/1)

Що означає: локатор знаходить рівно один елемент
Як перевіряють: findElements().size() == 1
У розмові:

“Make sure the locator is unique.”

1️⃣1️⃣ Test stability

Що означає: тести стабільно проходять на різних середовищах
У розмові:

“We’re improving test stability.”

1️⃣2️⃣ Synchronization issue

Що означає: тест швидший за UI
Причина: немає правильних waits
У розмові:

“The failure is due to synchronization issues.”

1️⃣3️⃣ Regression

Що означає: перевірка, що старий функціонал не зламався
У розмові:

“This is part of regression suite.”

1️⃣4️⃣ Smoke test

Що означає: мінімальний набір тестів “чи взагалі працює”
У розмові:

“Run smoke tests after deployment.”

1️⃣5️⃣ Test maintenance

Що означає: підтримка тестів, коли UI змінюється
У розмові:

“Locator changes increased test maintenance cost.”